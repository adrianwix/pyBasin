# Example: Getting Started with pyBasin

This notebook demonstrates the basic usage of the pyBasin library.

## Installation

If you haven't installed pybasin yet:

```bash
pip install pybasin
```

Or for development:

```bash
git clone https://github.com/adrianwix/pyBSTAB.git
cd pyBasinWorkspace
uv pip install -e .
```

## Basic Example

Let's estimate the basin stability of a simple 2D dynamical system.
